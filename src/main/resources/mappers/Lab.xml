<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thekiban.Mapper.LabMapper">
	<!-- 분석 파일 등록 -->
	<insert id="InsertAnalysisFile" parameterType="AnalysisFile">
		INSERT INTO analysis_file
		(user_id, analysis_file, analysis_origin_file, create_date, modify_date)
		VALUES
		(#{user_id}, #{analysis_file}, #{analysis_origin_file}, NOW(), NOW())
	</insert>
</mapper>